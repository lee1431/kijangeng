fetch('./json/notices.json').then(r=>{if(!r.ok)throw Error(r.status);return r.json()}).then(items=>{const list=document.getElementById('notice-list');if(!list)return;list.replaceChildren(...items.sort((a,b)=>new Date(b.date)-new Date(a.date)).slice(0,6).map(item=>{const row=document.createElement('div');row.className='py-3';const title=document.createElement('strong');title.textContent=item.title;const date=document.createElement('small');date.className='d-block text-muted';date.textContent=item.date;row.append(title,date);return row}))}).catch(console.error);
